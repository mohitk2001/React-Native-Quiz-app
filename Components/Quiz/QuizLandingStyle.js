import { StyleSheet } from "react-native";
const styles=StyleSheet.create({
    Landing_container:{
        width:"100%",
        display: "flex",
        alignItems: "center",
        backgroundColor:"red"
    }
})

export default styles